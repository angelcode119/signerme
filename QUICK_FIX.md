# 🔧 Quick Fix - حل مشکل فوری

## ⚠️ مشکل فعلی:

ربات اول و دوم از یک توکن استفاده می‌کنند!

## ✅ راه حل سریع:

### روش 1: استفاده از یک ربات (آسان‌ترین)

```bash
# فقط ربات اول
python m.py
```

یا

```bash
# فقط ربات دوم  
python bot2.py
```

### روش 2: توکن جداگانه (حرفه‌ای)

#### 1. ساخت ربات دوم:
- به `@BotFather` برو
- `/newbot` بفرست
- نام: APK Analyzer
- Username: مثلاً `myapkanalyzer_bot`

#### 2. تغییر توکن:
فایل `bot2.py` را باز کن، خط 32:

**قبل:**
```python
BOT2_TOKEN = BOT_TOKEN
```

**بعد:**
```python
BOT2_TOKEN = 'توکن_جدید_از_BotFather'
```

#### 3. اجرا:
```bash
python run_bots.py
```

## 🎯 تست:

### ربات اول (Generator):
1. `/start` → باید لیست APK ها بیاد
2. انتخاب APK → Quick یا Custom
3. ساخت APK

### ربات دوم (Analyzer):
1. `/start` → خوش‌آمد گویی
2. ارسال فایل APK → تحلیل
3. دریافت آیکون + اطلاعات

## ❌ اگه هنوز مشکل داری:

```bash
# حذف session ها
rm -f *.session*

# اجرای دوباره
python m.py
```
