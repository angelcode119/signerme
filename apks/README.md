# APK Files Directory 📱

این پوشه برای نگهداری فایل‌های APK پایه است که برای ساخت برنامه‌های سفارشی استفاده می‌شوند.

## راهنمای استفاده 🎯

### 1. اضافه کردن APK
فایل‌های APK خود را در این پوشه قرار دهید:
```
apks/
  ├── app1.apk
  ├── app2.apk
  └── game.apk
```

### 2. ثبت APKها در سیستم
دو روش برای ثبت APKها وجود دارد:

#### روش 1: اسکن خودکار (توصیه می‌شود)
1. وارد پنل ادمین شوید: `/admin`
2. روی **APK Management** کلیک کنید
3. دکمه **🔍 Scan Folder** را بزنید
4. تمام APKهای موجود در این پوشه اسکن و ثبت می‌شوند

#### روش 2: آپلود از طریق بات
1. وارد پنل ادمین شوید: `/admin`
2. روی **APK Management** کلیک کنید
3. دکمه **➕ Upload APK** را بزنید
4. فایل APK را ارسال کنید
5. بات خودکار آن را در این پوشه ذخیره و ثبت می‌کند

### 3. نکات مهم ⚠️

- **حجم فایل**: هر APK نباید بیشتر از 100 مگابایت باشد
- **نام فایل**: از نام‌های معنادار استفاده کنید (مثلاً `telegram.apk` به جای `app123.apk`)
- **فرمت**: فقط فایل‌های با پسوند `.apk` پشتیبانی می‌شوند
- **دسترسی**: اطمینان حاصل کنید که بات به این پوشه دسترسی خواندن/نوشتن دارد

### 4. مدیریت APKها 🛠️

بعد از اضافه کردن APKها می‌توانید:
- ✅ فعال/غیرفعال کردن APKها
- 📝 ویرایش نام نمایشی
- 📊 مشاهده آمار استفاده
- 🗑️ حذف APKها

### 5. نحوه کار سیستم 🔄

```
User Request → APK Selection → Build Custom APK → Sign → Deliver
```

1. کاربر APK مورد نظر را انتخاب می‌کند
2. سیستم APK پایه را از این پوشه می‌خواند
3. تغییرات سفارشی (theme، token) اعمال می‌شود
4. APK جدید ساخته و امضا می‌شود
5. به کاربر تحویل داده می‌شود

## مثال‌های APK مناسب 📱

- **Telegram**: برنامه‌های مسنجر
- **Instagram**: شبکه‌های اجتماعی
- **بازی‌ها**: APK بازی‌های اندروید
- **ابزارها**: برنامه‌های کاربردی

## عیب‌یابی 🔧

### APKها در لیست نمایش داده نمی‌شوند؟
1. مطمئن شوید فایل‌ها در پوشه `apks/` هستند
2. از پنل ادمین **🔍 Scan Folder** را بزنید
3. لاگ‌ها را بررسی کنید: `bot.log`

### خطای "APK not found"؟
- بررسی کنید که فایل هنوز موجود است
- دسترسی‌های فایل را چک کنید
- بات را ری‌استارت کنید

## پشتیبانی 💬

برای مشکلات و سوالات، لاگ‌های بات را بررسی کنید یا به توسعه‌دهنده مراجعه کنید.

---

**نکته**: این پوشه فقط برای APKهای پایه است. APKهای ساخته شده توسط کاربران در پوشه `builds/` ذخیره می‌شوند.
