# 📁 ساختار پروژه

## 🗂️ ساختار کلی

```
APK-Studio/
│
├── 🤖 bots/                    # ربات‌ها
│   ├── bot1_generator.py       # ربات ساخت APK
│   └── bot2_analyzer.py        # ربات تحلیل APK
│
├── 📦 modules/                 # ماژول‌های اصلی
│   ├── __init__.py
│   ├── auth.py                 # احراز هویت
│   ├── config.py               # تنظیمات
│   ├── utils.py                # توابع کمکی
│   ├── queue_manager.py        # مدیریت صف
│   ├── theme_manager.py        # مدیریت تم
│   ├── apk_builder.py          # ساخت APK
│   ├── apk_selector.py         # انتخاب APK
│   ├── apk_analyzer.py         # تحلیل APK
│   ├── apk_downloader.py       # دانلود APK
│   └── custom_build_handler.py # مدیریت ساخت شخصی
│
├── 📊 data/                    # داده‌ها
│   ├── users.json              # کاربران ربات 1
│   └── users2.json             # کاربران ربات 2
│
├── 📝 logs/                    # لاگ‌ها
│   ├── bot.log                 # لاگ ربات 1
│   └── bot2.log                # لاگ ربات 2
│
├── 🔨 builds/                  # فایل‌های ساخت موقت
│   └── (temporary build files)
│
├── 📥 downloads/               # دانلودهای موقت
│   └── (temporary downloads)
│
├── 📱 apks/                    # فایل‌های APK پایه
│   └── README.md
│
├── 📖 docs/                    # مستندات
│   ├── README.md
│   ├── START_BOTS.md
│   ├── QUICK_FIX.md
│   ├── IMPORTANT_BOT2_TOKEN.md
│   └── PROJECT_STRUCTURE.md
│
├── 🚀 run.py                   # اجرای ربات‌ها
├── 📋 requirements.txt         # کتابخانه‌ها
└── 🔒 .gitignore              # فایل‌های ignore

```

## 🎯 توضیح پوشه‌ها

### 🤖 bots/
ربات‌های اصلی که با کاربر تعامل دارند:
- **bot1_generator.py**: ساخت APK با شخصی‌سازی
- **bot2_analyzer.py**: تحلیل و استخراج اطلاعات APK

### 📦 modules/
ماژول‌های قابل استفاده مجدد:
- **auth.py**: سیستم احراز هویت OTP
- **config.py**: تنظیمات و متغیرهای محیطی
- **utils.py**: توابع کمکی عمومی
- **queue_manager.py**: مدیریت صف و محدودیت همزمانی
- **theme_manager.py**: مدیریت شخصی‌سازی تم
- **apk_builder.py**: منطق ساخت APK
- **apk_selector.py**: انتخاب و مدیریت APK ها
- **apk_analyzer.py**: تحلیل و استخراج از APK
- **apk_downloader.py**: دانلود APK از منابع مختلف
- **custom_build_handler.py**: مدیریت فرآیند ساخت شخصی

### 📊 data/
ذخیره داده‌های دائمی:
- فایل‌های JSON کاربران
- تنظیمات ذخیره شده

### 📝 logs/
لاگ‌های سیستم برای debug

### 🔨 builds/ & 📥 downloads/
فایل‌های موقت (پاک می‌شوند)

### 📱 apks/
APK های پایه برای ساخت

### 📖 docs/
تمام مستندات پروژه

## 🚀 نحوه استفاده

### اجرای ساده:
```bash
python run.py
```

### اجرای جداگانه:
```bash
# فقط ربات 1
python bots/bot1_generator.py

# فقط ربات 2
python bots/bot2_analyzer.py
```

## 📝 Import ها

همه ماژول‌ها از پوشه `modules` import می‌شوند:

```python
from modules.auth import UserManager
from modules.config import API_ID, API_HASH
from modules.utils import cleanup_session
```

## 🔧 توسعه

برای اضافه کردن ماژول جدید:
1. فایل را در `modules/` بسازید
2. در `modules/__init__.py` اضافه کنید
3. از طریق `modules.module_name` import کنید

## 🎨 مزایای ساختار جدید

✅ **سازماندهی بهتر** - همه چیز سر جای خودش
✅ **ماژولار** - قابل استفاده مجدد
✅ **قابل توسعه** - راحت اضافه کردن قابلیت جدید
✅ **تمیز** - root پوشه خلوت
✅ **حرفه‌ای** - استاندارد صنعت
