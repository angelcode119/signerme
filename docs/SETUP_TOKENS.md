# 🔑 تنظیم توکن‌های ربات

## ⚠️ مهم: دو توکن جداگانه لازم است!

شما باید **دو ربات جداگانه** در تلگرام بسازید.

---

## 📝 مراحل:

### 1️⃣ ربات اول (Generator) - قبلاً ساخته‌اید ✅

توکن فعلی شما:
```
7369619847:AAECDyBuAyntBsgT00JGre2jcLnXTDJUxPA
```

این در `modules/config.py` هست و **صحیح** است.

---

### 2️⃣ ربات دوم (Analyzer) - باید بسازید 🔨

#### مراحل ساخت:

1. به تلگرام بروید
2. پیدا کنید: `@BotFather`
3. بفرستید: `/newbot`
4. نام ربات: `APK Analyzer` (یا هر نام دیگری)
5. Username ربات: `myapkanalyzer_bot` (باید منحصر به فرد باشد)
6. توکن جدید را کپی کنید

مثال توکن:
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567
```

---

### 3️⃣ قرار دادن توکن دوم

فایل `modules/config.py` را باز کنید و خط 6 را پیدا کنید:

**قبل:**
```python
BOT2_TOKEN = 'YOUR_BOT2_TOKEN_HERE'  # Bot 2 - Analyzer (عوض کن!)
```

**بعد:**
```python
BOT2_TOKEN = '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567'  # توکن واقعی شما
```

---

## ✅ بررسی نهایی

فایل `modules/config.py` باید شبیه این باشد:

```python
API_ID = 5099517
API_HASH = '3bffbb2ff1f15e5812fbeb8ab22d0f66'

# Bot Tokens
BOT_TOKEN = '7369619847:AAECDyBuAyntBsgT00JGre2jcLnXTDJUxPA'  # ربات 1 ✅
BOT2_TOKEN = '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567'   # ربات 2 ✅

API_BASE_URL = "http://95.134.130.160:8765"
BOT_IDENTIFIER = "Generator Apk"
```

---

## 🚀 اجرا

بعد از تنظیم توکن:

```bash
python run.py
```

---

## 🔍 تست

### ربات 1 (Generator):
- باید لیست APK ها را نشان دهد
- ساخت APK با شخصی‌سازی

### ربات 2 (Analyzer):
- باید منتظر فایل APK باشد
- تحلیل و ارسال اطلاعات

---

## ❌ مشکلات رایج

### "Conflict: terminated by other getUpdates request"
→ دو ربات از یک توکن استفاده می‌کنند!
→ باید توکن دوم را در `modules/config.py` تغییر دهید

### "Invalid bot token"
→ توکن اشتباه است
→ دوباره از @BotFather بگیرید

---

## 💡 یادآوری

⚠️ هر ربات نیاز به توکن مستقل دارد
⚠️ نمی‌توانید از یک توکن برای دو ربات استفاده کنید
⚠️ توکن‌ها را محرمانه نگه دارید
