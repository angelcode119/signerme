# 🚀 نصب و راه‌اندازی

## 📥 دانلود پروژه

### روش 1: Clone جدید (توصیه می‌شود)

```bash
cd ~/Desktop

# حذف نسخه قدیمی (اگر دارید)
rm -rf signerme signerme-main

# دانلود نسخه جدید
git clone https://github.com/angelcode119/signerme.git

# ورود به پوشه
cd signerme
```

### روش 2: آپدیت نسخه موجود

```bash
cd مسیر/پروژه

# دریافت آخرین تغییرات
git fetch origin
git reset --hard origin/main
```

---

## 📦 نصب کتابخانه‌ها

```bash
pip install -r requirements.txt
```

یا:

```bash
pip install telethon aiohttp
```

---

## ⚙️ تنظیمات

### 1️⃣ توکن ربات‌ها

#### ربات اول (Generator):
فایل `modules/config.py` را باز کنید:

```python
BOT_TOKEN = 'YOUR_BOT1_TOKEN'  # توکن ربات اول
```

#### ربات دوم (Analyzer):
فایل `bots/bot2_analyzer.py` را باز کنید، خط 36:

```python
BOT2_TOKEN = 'YOUR_BOT2_TOKEN'  # توکن ربات دوم
```

⚠️ **مهم:** اگر می‌خواهید هر دو ربات همزمان کار کنند، باید توکن‌های جداگانه داشته باشید!

### 2️⃣ ساخت ربات دوم (اختیاری)

اگر می‌خواهید ربات دوم هم داشته باشید:

1. به `@BotFather` بروید
2. `/newbot` بفرستید
3. نام: `APK Analyzer`
4. Username: `your_analyzer_bot`
5. توکن را در `bots/bot2_analyzer.py` قرار دهید

---

## 🚀 اجرا

### اجرای هر دو ربات:

```bash
python run.py
```

### اجرای جداگانه:

```bash
# فقط ربات 1
python bots/bot1_generator.py

# فقط ربات 2
python bots/bot2_analyzer.py
```

---

## 📁 ساختار پوشه‌ها

بعد از اولین اجرا:

```
signerme/
├── data/              # Session ها و داده‌ها
│   ├── bot1_session.session
│   ├── bot2_session.session
│   ├── users.json
│   └── users2.json
│
├── logs/              # لاگ‌ها
│   ├── bot.log
│   └── bot2.log
│
├── builds/            # فایل‌های موقت build
└── downloads/         # دانلودهای موقت
```

همه این پوشه‌ها **خودکار** ساخته می‌شوند.

---

## 🔧 عیب‌یابی

### خطای Import:

```bash
pip install --upgrade telethon aiohttp
```

### خطای Permission:

```bash
chmod +x run.py
chmod +x bots/*.py
```

### حذف Session ها:

```bash
rm -f data/*.session*
```

### مشاهده لاگ‌ها:

```bash
tail -f logs/bot.log      # ربات 1
tail -f logs/bot2.log     # ربات 2
```

---

## ✅ تست

### تست ربات 1 (Generator):
1. `/start` → باید لیست APK ها بیاید
2. انتخاب APK → گزینه Quick یا Custom
3. ساخت موفق

### تست ربات 2 (Analyzer):
1. `/start` → پیام خوش‌آمد
2. ارسال فایل APK
3. دریافت آیکون + اطلاعات

---

## 💡 نکات

✅ Session ها در `data/` ذخیره می‌شوند
✅ لاگ‌ها در `logs/` ذخیره می‌شوند
✅ فایل‌های موقت خودکار پاک می‌شوند
✅ Git فایل‌های حساس را ignore می‌کند

---

## 🆘 کمک

اگر مشکلی داشتید:
1. لاگ‌ها را چک کنید
2. Session ها را پاک کنید
3. دوباره clone کنید
